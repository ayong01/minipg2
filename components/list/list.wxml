<!--components/list/list.wxml-->
<view class="container">
 <block wx:for="{{list}}" wx:key="index" wx:item="item">
  <view class="box {{currentindex == index ? 'active' : ''}}" data-index="{{index}}" data-item="{{item}}" bind:tap="handelclick"><text>{{item}}</text></view>
 </block>
</view>